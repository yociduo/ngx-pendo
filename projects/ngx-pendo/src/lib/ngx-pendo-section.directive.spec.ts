import { NgxPendoSectionDirective } from './ngx-pendo-section.directive';

describe('NgxPendoSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxPendoSectionDirective();
    expect(directive).toBeTruthy();
  });
});
