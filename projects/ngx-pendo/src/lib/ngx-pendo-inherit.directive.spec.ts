import { NgxPendoInheritDirective } from './ngx-pendo-inherit.directive';

describe('NgxPendoInheritDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxPendoInheritDirective();
    expect(directive).toBeTruthy();
  });
});
